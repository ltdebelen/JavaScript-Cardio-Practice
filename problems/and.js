// Make a function that evaluates and returns the value of an AND logical operator based on the parameters passed.

// Ex: and(true, false) ➞ false
// and(true, true) ➞ true

function and(first, sec) {
  return first && sec;
}

console.log(and(true, true));
