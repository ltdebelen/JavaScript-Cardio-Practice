// Create a function that takes height and width and finds the perimeter of a rectangle.

// Ex: findPerimeter(6, 7) ➞ 26

function findParameter(h, w) {
  return h * 2 + w * 2;
}

console.log(findParameter(6, 7));
