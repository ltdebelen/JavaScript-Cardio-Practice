// Given two strings, firstName and lastName, return a single string in the format "last, first".

// Ex: concatName("First", "Last") ➞ "Last, First"

function concatName(first, last) {
  return first + ", " + last;
}

console.log(concatName("First", "Last"));
