// Create a function that takes a string and returns it as an integer.

// Ex: stringInt("6") ➞ 6; stringInt("1000") ➞ 1000

function stringInt(strNum) {
  return parseInt(strNum);
}

console.log(stringInt(6));
